import 'package:flutter/material.dart';

/// bottom navigation bar 가 들어갈 screen UI
class DashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Dashboard Screen'),
        ),
      ),
    );
  }
}
